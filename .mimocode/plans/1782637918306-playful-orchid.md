# Plan: Further Improvements After DESIGN.md Refactoring

## What was already done
- DESIGN.md created with shadcn/ui token system
- CSS custom properties added to :root and [data-dark-mode]
- Hardcoded hex colors replaced with semantic tokens in 8+ SCSS files
- Dark mode consolidated (~40% rules updated to CSS vars)
- Bug fixes: duplicate $black, invalid text-shadow, transition without property
- Unused SCSS variables removed

## Remaining Improvement Opportunities

### 1. Dark Mode SCSS Variables Cleanup (_dark.scss)

**Lines 5-20** still define SCSS variables that are only used in 2 places (btn-primary, btn-outline-primary):

```
$body-bg-dark: $gray-900;
$body-overlay-dark: darken($body-bg-dark, 2.5%);
$body-color-dark: $gray-300;
$dots-dark: darken($body-color-dark, 50%);
$link-color-dark: $brand;
$button-color-dark: lighten($brand, 1%);
$focus-color-dark: lighten($link-color-dark, 2.5%);
$navbar-dark-color, $navbar-dark-hover-color, $navbar-dark-active-color
```

**Action**: Replace remaining SCSS variable references with CSS custom properties where possible. Keep only `$button-color-dark` for Bootstrap's `button-variant()` mixin (can't replace that).

**Files**: `assets/scss/common/_dark.scss`

### 2. SCSS $secondary Conflict

`$secondary: $gray-700` in `_variables.scss` (line 120) conflicts with DESIGN.md `--secondary: #f4f4f5`. Bootstrap uses `$secondary` for button variants and other components.

**Action**: Rename SCSS `$secondary` to `$secondary-dark` or similar to avoid confusion, and ensure Bootstrap button secondary uses the correct token.

**Files**: `assets/scss/common/_variables.scss`

### 3. _buttons.scss Hardcoded Values

Still uses SCSS variables directly instead of CSS custom properties:

- Line 2: `$navbar-light-color` → `var(--foreground)`
- Line 31: `$navbar-light-hover-color` → `var(--primary)`
- Line 35: `$navbar-light-active-color` → `var(--primary)`
- Lines 74-96: `$color-btn-text`, `$color-btn-bg`, `$color-btn-border` → CSS vars
- Line 140: `$body-color` → `var(--foreground)`
- Line 181: `$link-color` → `var(--primary)`
- Line 243: `$primary` → `var(--primary)`

**Files**: `assets/scss/components/_buttons.scss`

### 4. _sidebar.scss Hardcoded Values

- Lines 4, 14, 19, 25: `$white` → `var(--background)`
- Lines 30, 35: `$gray-200` → `var(--border)`
- Lines 55, 71, 80: `$gray-200` → `var(--border)`
- Lines 59, 75, 85, 103: `$body-color` → `var(--foreground)`
- Line 85: `$link-color` → `var(--primary)`

**Files**: `assets/scss/layouts/_sidebar.scss`

### 5. _footer.scss Hardcoded Values

- Line 2: `$gray-200` → `var(--border)`

**Files**: `assets/scss/layouts/_footer.scss`

### 6. _global.scss Remaining Hardcoded Values

- Line 60: `$gray-300` → `var(--border)`
- Line 163: `$gray-200` → `var(--border)`
- Line 238: `$gray-200` → `var(--border)`
- Line 270: `$primary` → `var(--primary)`
- Line 275: `rgba(212, 53, 159, 0.2)` → keep as is (selection color, decorative)
- Line 279: `$gray-300` → `var(--border)`
- Line 321: `#fff` → `var(--background)`
- Lines 300-301, 316: `$primary`, `$brand`, `$blue-300` → keep (gradient/decorative)

**Files**: `assets/scss/common/_global.scss`

### 7. _search.scss Hardcoded Values

- Line 30: `$gray-100` → `var(--secondary)`
- Lines 35, 47, 80: `$gray-200` → `var(--border)`
- Line 57: `$black` → `var(--foreground)`
- Line 62: `$gray-700` → `var(--muted-foreground)`

**Files**: `assets/scss/components/_search.scss`

### 8. _header.scss Remaining Hardcoded Values

- Line 63: `$gray-200` → `var(--border)`
- Line 140: `$gray-200` → `var(--border)`
- Line 243: `$gray-700` → `var(--muted-foreground)`
- Line 342: `$black` → `var(--foreground)`
- Line 455: `$pink-100` → keep (decorative hover bg)

**Files**: `assets/scss/layouts/_header.scss`

### 9. Accessibility Improvements

- Add `prefers-reduced-motion` media query to disable animations
- Ensure focus-visible states are visible (currently `outline: 0` on some elements)
- Add skip-to-content link

**Files**: `assets/scss/common/_global.scss`, `layouts/_default/baseof.html`

### 10. Performance: Remove Unused CSS

- Run PurgeCSS or similar to remove unused Bootstrap utilities
- The `$enable-*` flags in `_variables.scss` can disable unused Bootstrap components

**Files**: `assets/scss/common/_variables.scss`

---

## Priority Order

| Priority | Items | Impact |
|----------|-------|--------|
| High | #1, #3, #4, #6, #7, #8 | Consistency — all colors via CSS vars |
| Medium | #2, #5 | Clean code — remove conflicts |
| Low | #9, #10 | Polish — a11y and performance |

## Verification

1. `npm run dev` — no SCSS errors
2. Visual check: light mode, dark mode, all page types
3. `npm run lint:styles` — reduced warnings
