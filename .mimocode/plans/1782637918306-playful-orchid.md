# Plan: Final cleanup

## 1. SCSS: оставшиеся $gray-* → CSS vars

### `_details.scss`
- Строка 3: `$gray-200` → `var(--border)`
- Строка 30: `$gray-100` → `var(--secondary)`
- Строка 56: `$gray-300` → `var(--border)`

### `_alerts.scss`
- Строка 3: `$font-size-sm` → `0.875rem`

## 2. Удалить KaTeX шрифты (если отключён)

`kaTex = false` в params.toml → `static/fonts/KaTeX_*` не нужны.

## Verification

1. `hugo` — сборка
2. `npm test` — lint
